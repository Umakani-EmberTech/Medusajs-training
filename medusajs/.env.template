STORE_CORS=http://localhost:8000,https://docs.medusajs.com
ADMIN_CORS=http://localhost:5173,http://localhost:9000,https://docs.medusajs.com
AUTH_CORS=http://localhost:5173,http://localhost:9000,https://docs.medusajs.com
# Optional for local dev (Medusa uses in-memory fake Redis if unset). Example:
# REDIS_URL=redis://127.0.0.1:6379
JWT_SECRET=supersecret
COOKIE_SECRET=supersecret
# Use the real password for the "postgres" user (the one you chose when installing PostgreSQL on Windows).
DATABASE_URL=postgres://postgres:postgres@123@127.0.0.1:5432/postgres
DB_NAME=medusa-v2
